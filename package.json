{
  "name": "html-ng-temp",
  "version": "0.1.0",
  "description": "Parses HTML for email like if is Angular templates, and apply CSS as inline styles in it.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "mocha -r ts-node/register './src/**/*.test.ts'",
    "prepare": "npm run clean && tsc",
    "build": "npm run clean && tsc",
    "clean": "rm -rf ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sleep-written/html-ng-temp.git"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "email",
    "html",
    "parser",
    "json"
  ],
  "author": "sleep-written",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sleep-written/html-ng-temp/issues"
  },
  "homepage": "https://github.com/sleep-written/html-ng-temp#readme",
  "dependencies": {
    "node-html-parser": "^5.2.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.19",
    "chai": "^4.3.6",
    "mocha": "^9.2.1",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
